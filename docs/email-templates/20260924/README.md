# Nature + Finale daily mailers — 2026-09-24

This immutable snapshot preserves the 18 production Daily SineDay aliases, subjects, preheaders, copy, sender, Reply-To, variables, wave-position images, CTA, opt-out markup, and plain text from `20260923`.

Each numbered HTML source changes only its hero to the matching `SineDaySceneN.png`: a deterministic 752 × 752 composite of canonical `DayN.jpeg` nature artwork and the approved transparent `SineDuckFinaleN.png`. Heroes render at 464 × 464 with a Day-specific alt and `data-sineday-surface="day-scene"`.

Run `npm run compose:daily-email-scenes` to verify canonical source hashes and regenerate all 18 scenes plus `asset-manifest.json`. Deploy and verify every production scene URL before updating any live Resend alias. Welcome and Confirmation are intentionally outside this snapshot and remain unchanged.
